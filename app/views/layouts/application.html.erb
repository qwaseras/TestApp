<!DOCTYPE html>
<html>
<%= render 'layouts/head'%>
<%= render 'layouts/header'%>

  <body>
  <div class="container">
      <% flash.each do |key, value| %>
        <div class="alert alert-<%= key %>"><%= value %></div>
      <% end %>
    <%= yield %>
  </body>
</html>
